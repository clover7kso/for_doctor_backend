# Migration `20200918054440-init`

This migration has been generated by woony1 at 9/18/2020, 2:44:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `Doctor`.`Product` DROP COLUMN `ad`,
ADD COLUMN `aboveAD` boolean  NOT NULL DEFAULT false,
ADD COLUMN `cateAD` boolean  NOT NULL DEFAULT false
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200918044134-init..20200918054440-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model HomeAD {
   id        String    @id @default(cuid())
@@ -68,19 +68,8 @@
   post   Post   @relation(fields: [postId], references: [id])
   postId String
 }
-model ProductAD {
-  id        String    @id @default(cuid())
-  updatedAt DateTime  @updatedAt @default(now())
-  createdAt DateTime  @default(now())
-  expireAt  DateTime?
-
-  category  String
-  product   Product @relation(fields: [productId], references: [id])
-  productId String
-}
-
 model Product {
   id        String    @id @default(cuid())
   updatedAt DateTime  @updatedAt @default(now())
   createdAt DateTime  @default(now())
@@ -93,13 +82,13 @@
   title        String
   content      String        @default("")
   views        Int
   calls        Int
-  ad           Boolean
+  aboveAD      Boolean       @default(false)
+  cateAD       Boolean       @default(false)
   company      String
   phone        String
   likeProduct  likeProduct[]
-  ProductAD    ProductAD[]
 }
 model sampleImage {
   id        String   @id @default(cuid())
```


