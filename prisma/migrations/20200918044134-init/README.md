# Migration `20200918044134-init`

This migration has been generated by woony1 at 9/18/2020, 1:41:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Doctor`.`ProductAD` (
`id` varchar(191)  NOT NULL ,
`updatedAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`createdAt` datetime(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
`expireAt` datetime(3)  ,
`category` varchar(191)  NOT NULL ,
`productId` varchar(191)  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Doctor`.`ProductAD` ADD FOREIGN KEY (`productId`) REFERENCES `Doctor`.`Product`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200916072834-init..20200918044134-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model HomeAD {
   id        String    @id @default(cuid())
@@ -68,8 +68,19 @@
   post   Post   @relation(fields: [postId], references: [id])
   postId String
 }
+model ProductAD {
+  id        String    @id @default(cuid())
+  updatedAt DateTime  @updatedAt @default(now())
+  createdAt DateTime  @default(now())
+  expireAt  DateTime?
+
+  category  String
+  product   Product @relation(fields: [productId], references: [id])
+  productId String
+}
+
 model Product {
   id        String    @id @default(cuid())
   updatedAt DateTime  @updatedAt @default(now())
   createdAt DateTime  @default(now())
@@ -86,8 +97,9 @@
   ad           Boolean
   company      String
   phone        String
   likeProduct  likeProduct[]
+  ProductAD    ProductAD[]
 }
 model sampleImage {
   id        String   @id @default(cuid())
```


